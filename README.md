# Simple Calculator Project

This project includes a basic calculator implemented in Python and unit tests for its functions.

## Files:
- `calculator.py`: Contains the calculator functions (add, subtract, multiply, divide).
- `test_calculator.py`: Unit tests for the calculator functions using `unittest`.

## How to Run:
1. Run the calculator: `python calculator.py`
2. Run tests: `python test_calculator.py`
